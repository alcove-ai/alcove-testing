name: Test Pipeline
workflow:
  - id: greet
    type: agent
    agent: Issue Greeter
    trigger:
      github:
        events: [issues]
        actions: [labeled]
        labels: [test-pipeline]
        repos: [bmbouter/alcove-testing]
        delivery_mode: polling
    outputs: [comment_posted]
